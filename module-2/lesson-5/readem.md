## Git & Github

Commands: GIT

```

ls -a
rm -rf
cd ..
cd folder_name
mkdir folder_name
touch file.format

------------------

git init // install git repository
git add file_name.format or -A / . ( all  files added to the repository )
git status // check status git repository
git commit -m "message text"  / message text commit 
git log --> all commits list

git branch  -> check last branch
git branch "barnch name"

git checkout  "comit id" or "branch name"
git push origin "branch_name"
git pull orign "branch_name"


git config --global user.name "user name"
git config --global user.email "khodieff@gmail.com"

git config --list 



``